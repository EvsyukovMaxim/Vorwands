<Window x:Class="DoubleGIS.Vorwands.VorwandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoubleGIS.Vorwands"
        mc:Ignorable="d"
        Title="VorwandWindow" Height="753" Width="701">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBox Text="{Binding Name, StringFormat=Наименование зацепки: {0} }">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <EventSetter Event="LostFocus" Handler="TextBox_Editor"/>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <RichTextBox local:HtmlRichTextBoxBehavior.Text="{Binding Description}" Height="578" Margin="45,90,100,0"/>
        </StackPanel>
    </Grid>
</Window>
