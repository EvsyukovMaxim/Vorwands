<Window x:Class="DoubleGIS.Vorwands.VorwandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoubleGIS.Vorwands"
        mc:Ignorable="d"
        Title="VorwandWindow" Height="753" Width="701">
    <Grid >
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding VorwandName }" 
                       MouseDown="UIElement_OnMouseDown"
                       Visibility="{Binding IsReadonlyVisible}"></TextBlock>
            <StackPanel Orientation="Horizontal" Visibility="{Binding IsEditVisible}">
                <TextBox Text="{Binding VorwandName, UpdateSourceTrigger=PropertyChanged }"
                         Width="623"></TextBox>
                <Button x:Name="BtnOk" Content="OK" Click="Click_BtnOk"></Button>
                <Button x:Name="BtnCancel" Content="Cancel" Click="Click_BtnCancel"/>
            </StackPanel>
            <TextBlock Text="{Binding VorwandId,StringFormat=Id: {0}}" ></TextBlock>
            <RichTextBox local:HtmlRichTextBoxBehavior.Text="{Binding VorwandDescription}" Height="578" Margin="45,90,100,0"/>
        </StackPanel>
    </Grid>
</Window>
